- alias: Turn on led dome on movement
  trigger:
    platform: event
    event_type: doorbird_voordeur_motion
  condition:
  - condition: state
    entity_id: sun.sun
    state: below_horizon
  action:
    data:
      entity_id: switch.light_driveway_door
    service: homeassistant.turn_on
- alias: Turn off led dome after xx minutes
  trigger:
    entity_id: switch.light_driveway_door
    for:
      minutes: 3
    platform: state
    to: 'on'
  action:
    data:
      entity_id: switch.light_driveway_door
    service: homeassistant.turn_off

