db_url: !secret db_url
purge_keep_days: 14
purge_interval: 1
include:
  domains:
    - sensor
    - switch
    - media_player
    - device_tracker
    - automation
exclude:
  entities:
    - sensor.since_last_boot
    - sensor.date
    - sensor.pvoutput
    - sensor.ram_free
    - sensor.ram_use