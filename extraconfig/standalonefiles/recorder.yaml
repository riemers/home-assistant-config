db_url: !secret db_url
purge_keep_days: 14
purge_interval: 2
exclude:
  entities:
    - sensor.last_boot
    - sensor.wasautomaat_distance
  domains:
    - updater
    - camera
    - media_player
    - sun
    - automation
    - zwave
# include:
#   domains:
#   - binary_sensor
#   - sensor
#   - input_boolean
#   - input_select
#   - input_number