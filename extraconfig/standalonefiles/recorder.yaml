db_url: !secret db_url
purge_keep_days: 14
purge_interval: 2
exclude:
  entities:
    - sensor.last_boot
  domains:
    - zwave
include:
  domains:
  - sensor
  - input_boolean
  - input_select
  - input_number