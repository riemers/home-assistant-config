docker_containers:
  control: hidden
  icon: mdi:docker
  name: Docker Containers
  entities:
   - switch.doorbird
   - switch.haproxy
   - switch.mariadb
   - switch.pihole
   - switch.sabnzbd
   - switch.unifi

#To update go to template editor and run this:
# docker_containers:
#   control: hidden
#   icon: mdi:docker
#   name: Docker Containers
#   entities:
# {% for state in states.switch if state.attributes.icon == 'mdi:docker' %}    - {{ state.entity_id }}
# {% endfor %}